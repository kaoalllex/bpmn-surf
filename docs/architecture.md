# Architecture

Chrome Extension (Manifest V3) for visually comparing BPMN 2.0 and DMN diagrams in GitLab merge requests and repositories.

## What the project does

- Detects BPMN/DMN files on GitLab pages (MR and file view in a repository)
- Loads two versions of a diagram (MR branch vs. target branch) via the GitLab API
- Renders them with semantic differences highlighted (🟢 added, 🔴 removed)
- Supports comparison against a local file, zoom/pan, "Fit view", a properties panel, and downloading a version

## Directory structure

All application code is under `src/`; the root keeps only `manifest.json`, `version.json`, `CHANGELOG.md`, `libs/`, `icons/` (extension icons; placeholder in R1 of the bpmn-surf rebrand, UX-0009), `docs/`, `test/`, `scripts/`, `package*.json`. The layout reflects three script scopes: `core/` (shared), `content/` (the content script for GitLab pages), `differ/` (the separate differ tab), plus the extension context for the update mechanism — `background/` (service worker) and `popup/` (the icon popup window). File names are unique across the whole tree — in the table below the path is not duplicated, lookup is by name.

```
src/
  core/        config.js, models.js, utils.js          (cross-scope: data + utilities)
  content/     main.js, app.js, diff-params-builder.js, file-type-detector.js,
               page-reloader.js, camunda-bpmn-moddle-manager.js, content-styles.css
               (content-styles.css — styles for the buttons injected into GitLab's DOM)
    providers/ repo-provider.js, ui-repo-provider.js,
               repo-provider-factory.js, fallback-repo-provider.js
      gitlab/  gitlab-repo-provider-base.js, gitlab-api-repo-provider.js,
               gitlab-repo-provider.js, gitlab-ui-repo-provider.js,
               gitlab-url-parser.js, gitlab-dom-scraper.js,
               merged-mr-commit-resolver.js, master-commit-manager.js, single-entry-cache.js
      github/  (GitHub provider — populated in REFAC-0004 step 1.3+)
  update/      version-info.js, update-checker.js       (pure update logic, FEAT-0012)
  background/  update-service-worker.js                 (SW: version check + badge)
  popup/       popup.html, popup.js, popup.css           (update window opened from the icon)
  differ/      styles.css
    shared/    differ-params.js, diagram-versions.js, branch-indicator.js, diff-type.js,
               differ-loading-overlay.js, differ-empty-state.js, update-indicator.js,
               differ-tab-navigator.js
    bpmn/      bpmn-differ.js, bpmn-differ-view.js, bpmn-xml-comparator.js, diff-highlighter.js,
               changes-table-view.js, properties-panel-highlighter.js, properties-group-expander.js,
               condition-formatter.js, canvas-viewport.js, element-searcher.js, search-panel.js
    dmn/       dmn-differ.js, dmn-differ-view.js, dmn-table-viewport.js, dmn-xml-comparator.js, dmn-diff-painter.js
    navigation/ call-activity-locator.js, call-activity-navigator.js, caller-locator.js,
               decision-locator.js, decision-navigator.js, decision-caller-locator.js,
               back-navigator.js, handler-locator.js, handler-navigator.js,
               correlation-locator.js, correlation-navigator.js, process-file-index.js
    platform/  platform-client.js, gitlab-platform-client.js, github-platform-client.js,
               platform-client-factory.js
               (differ-scope platform seam, REFAC-0004; mirror of content/providers.
               github-platform-client.js is an inert stub until subtask 2)
```

The paths to these files are listed in four registries that must be kept in sync when moving/adding a file: `manifest.json#content_scripts` (order is critical) and `#web_accessible_resources`, `utils.js#loadScripts` (names = paths in `web_accessible_resources`, otherwise `chrome.runtime.getURL` returns empty), `test/support/scope.js#SCOPE_FILES`. The registries' synchronicity and the existence of the referenced files on disk are protected by the structural test `test/structure/registries.test.js` (otherwise a desync breaks silently at runtime while unit tests stay green) — see `docs/testing.md`.

## Structure

```
main.js → App (app.js) → Providers → Differs
                          |            ├─ bpmn-differ.js / dmn-differ.js (renders the diff in a separate page)
                          ├─ RepoProvider     (data/detection; interface — repo-provider.js)
                          │     └─ FallbackRepoProvider → [GitLabApiRepoProvider (MR API, primary), GitLabRepoProvider (DOM/heuristics, fallback), GitHubRepoProvider (inert stub, REFAC-0004)]
                          │            the two GitLab providers ↑ extend GitLabRepoProviderBase (shared keeper logic)
                          └─ UIRepoProvider   (button injection; interface — ui-repo-provider.js)
                                └─ GitLabUIRepoProvider (default) | GitHubUIRepoProvider (github.com, inert stub)
```

The `App` core knows nothing about GitLab: it works only through the neutral interfaces `RepoProvider`/`UIRepoProvider`; the concrete implementations are assembled by `repo-provider-factory.js` (`createRepoProvider`/`createUIRepoProvider`) and passed into `App` through its constructor (DI). `FallbackRepoProvider` is a whole-provider fallback: on `init()` it picks the first available and successfully initialized implementation and delegates all calls to it. `GitLabApiRepoProvider` (primary) resolves MR parameters via the GitLab MR API; if on an MR page the API is unavailable / has no `diff_refs`, its `init()` returns `false` and the chain falls back to the DOM/heuristic `GitLabRepoProvider`. Both GitLab providers inherit a shared base, `GitLabRepoProviderBase` (the keeper logic for page detection, URL parsing, project id resolution, and finding the selected file — what is identical for both paths), and hold collaborators via composition: `GitLabUrlParser` (pure URL/path parsing), `GitLabDomScraper` (all DOM reads of the GitLab page), `SingleEntryCache` (a "last value per key" cache). The DOM path additionally uses `MergedMrCommitResolver` (a heuristic for the target commit of a merged MR; "doomed" along with the DOM path once the API is debugged).

Flow: `app.js` listens to `mouseup`/`popstate`, detects the page → the provider resolves commits/branches and loads the content → the UI provider adds the accented "Schema diff"/"Decision diff" (MR) or "View schema"/"View decision" (repo) buttons → on click, `utils.js#openDiffer` opens the diff page, into which the libraries from `libs/` and the differ scripts are loaded. The differ page's parameters are assembled by `diff-params-builder.js` in a neutral form (sourceRef/targetRef/… + a `platform` descriptor), which are parsed and validated by `DifferParams`.

**Three script scopes** (do not confuse them):
1. **GitLab page content scripts** — the order is set in `manifest.json#content_scripts` (app, providers, utilities).
2. **The differ page** (a separate tab) — scripts are loaded via `utils.js#loadScripts` in the order: `utils.js` → class files → `bpmn-differ.js` → `dmn-differ.js`. They all share the single global scope of this tab. A new JS file for the differ page must be added both to `loadScripts` and to the manifest's `web_accessible_resources`. ⚠️ The differ page is a `window.open('about:blank')` (see `utils.js#openDiffer`): an ordinary web context with no access to `chrome.*`. Data from "outside" arrives only through postMessage parameters from the content script; feedback goes back via `window.opener` or by opening a web-accessible extension page.
3. **The update extension context (FEAT-0012)** — the service worker (`background/update-service-worker.js`) and the popup (`popup/`). They have full access to `chrome.*`. The SW pulls in `core/config.js` + `update/*` via `importScripts`; the popup via `<script>`. These files are NOT part of the four differ/content registries (they live in `manifest#action`/`#background`, and `popup.html` is additionally in `web_accessible_resources` — so the differ page can open it via `window.open`). Update flow: on a `chrome.alarms` schedule the SW fetches `version.json` → compares it with `manifest.version` → writes the state to `chrome.storage.local` and sets a badge; the popup and the indicator in the differ toolbar display this and walk the user through the update (`git pull` + `chrome.runtime.reload`). The extension does not replace its own files itself (a load-unpacked limitation) — it only notifies.

**Shared differ-page classes**: `bpmn-differ.js` (`BpmnDiffer`) and `dmn-differ.js` (`DmnDiffer`) are orchestrators, both using the shared classes `DifferParams` (differ-params.js), `DiagramVersions` (diagram-versions.js), `BranchIndicator` (branch-indicator.js), as well as `DiffType` (diff-type.js). There is no global mutable state on the differ page — all state lives in class fields, dependencies are passed through constructors. When changing the shared classes, check both the BPMN and the DMN diff.

**Differ-scope platform seam** (`src/differ/platform/`, REFAC-0004): every platform-specific data access the differ page performs (raw/blob URLs, code search, PR/MR changed-files) is behind the `PlatformClient` interface — the differ mirror of the content scope's `RepoProvider`. Each orchestrator builds one client via `createPlatformClient(params.platform)` (chosen by `platform.kind`) and injects it into `DiagramVersions` and every navigation locator, which keep their higher-level logic (caches, the exact-term gate, classification) on the normalised hit shape. So the locators carry no GitLab URL knowledge; adding GitHub is a new `GitHubPlatformClient` + one factory case.

## Key files

| File | Purpose |
|------|-----------|
| `main.js` | Entry point, initializes App |
| `app.js` | Main application class, orchestration |
| `bpmn-differ.js` | `BpmnDiffer` — the BPMN diff orchestrator + bootstrap (message listener) |
| `bpmn-differ-view.js` | `BpmnDifferView` — the DOM of the BPMN differ page (layout, header, footer, buttons). The header is a flex toolbar (`.differ-toolbar`) with `.differ-btn-group` groups (file + download · branch indicator · Switch branch · view · back navigation · Close); download/zoom/highlight/close are the icons `↓`/`+`/`−`/`⤢`/`☀☼`(toggle)/`✕` (UX-0007). The back-navigation group (`BackNavigator`, FEAT-0023, icon `⤴`) is mounted via `setBackNavigator()` before `build()` and rendered just before Close. The right block is pinned to the edge (`.differ-toolbar-spacer`), `Switch branch` is a separate group at its start (fixed position, not shifted by the branch name length). The properties panel is resizable: between the canvas and `propsCell` there is a `<td>` splitter `.differ-splitter` (drag → width of `propsCell`), the width is clamped by `BpmnDifferView.clampPanelWidth` (unit-tested) and remembered in `localStorage[bpmnDiffer.propsWidth]`; "Hide properties" hides both the panel and the splitter |
| `differ-params.js` | `DifferParams` — parsing/validation of the differ page's parameters from postMessage (shared by BPMN and DMN). Neutral fields + a `platform` descriptor; `requirePlatformInfo()` (BPMN), `isSourceVersionDefined()`, `rawFileUrl(ref, filePath=this.filePath)`, `toNestedDifferParams(filePath, fileName, extra)` (the nested Call Activity / caller differ; `extra` carries the FEAT-0023 direction hint). `divedInFrom` (`{filePath,fileName}` | null) — the calling diagram we dived in from (its tab is open above us), set only on dive-in (down); enables dive-out to jump straight back up to it. `selectCalledProcessIds` (`string[]` | null) — set on dive-out (up): the opened caller auto-selects its Call Activity that calls one of these process ids, highlighting the call site. `targetFilePath` (defaults to `filePath`) — the path of the target side, differs only when a file is renamed in the MR (BUG-0002); `targetFileName` = basename(`targetFilePath`) — the file name of the target side for the title and download (when renamed ≠ `fileName`) |
| `diagram-versions.js` | `DiagramVersions` — loading/storing/downloading the diagram versions (shared). `loadBranchXml()` loads the target side by `params.targetFilePath` (may differ from `filePath` on rename, BUG-0002), `loadMrXml()` — by `filePath`. `download(content, branchName, fileName=params.fileName)` — the file name is passed by the side (target → `targetFileName`, source → `fileName`) |
| `branch-indicator.js` | `BranchIndicator` — the label of the displayed version in the header (the branch name or commit message + short id) + a word for the side's role, so it reads without color: target → `Original`, source → `Changed` (in branch-view without a second side the prefix is not shown); colors (shared); `setShownLabel`/`isTargetBranchShown`. `setAbsentLabel(targetSide)` — a dimmed italic label for the side where the file does not exist (UX-0003), keeping `isTargetBranchShown()` correct. Side-specific text: empty source → `file deleted` (removed in the MR), empty target → `file does not exist` (a new file) |
| `diff-type.js` | `DiffType` — diff types with colors (shared by BPMN and DMN) |
| `differ-loading-overlay.js` | `DifferLoadingOverlay` — a full-screen spinner shown while the differ page renders (UX-0008). Shared by BPMN and DMN: shown in the view's `build()`, hidden in `showCanvas()`; covers the blank tab of a freshly opened (including a nested Call Activity) differ. The long processId→file resolution runs on the source page and is covered there by the badge spinner in `CallActivityNavigator` |
| `differ-empty-state.js` | `DifferEmptyState` — a placeholder inside the canvas cell (shared, UX-0003 / BUG-0001). Unlike the full-screen `DifferLoadingOverlay`, it lives in the canvas cell, so the toolbar stays available. An empty message = a plain cover (switching to a missing side; `dmn-js` has no `clear()`), a non-empty one = centered text (the file is absent in both versions). The views provide `showEmptyState`/`hideEmptyState` |
| `bpmn-xml-comparator.js` | `BpmnXmlComparator` — a semantic comparison of two BPMN XMLs. Besides the diff groups (`nodeIdToDiffsMap`) and conditions (`nodeIdToConditions`), `compare()` also returns `nodeIdToMappingChanges` (id → Map(list-group name → `[{label, changed}]`)) for per-element highlighting of In/Out mappings and Inputs/Outputs entries |
| `diff-highlighter.js` | `DiffHighlighter` — coloring diff elements and highlight markers |
| `changes-table-view.js` | `ChangesTableView` — the changes table in the footer |
| `properties-panel-highlighter.js` | `PropertiesPanelHighlighter` — highlighting groups in the properties panel, conditions. For list groups (In/Out mappings, Inputs/Outputs), on top of highlighting the group header it additionally colors the specific changed entries (by `nodeIdToMappingChanges` from the comparator, matched by the entry header text): changed→blue, added→green (MR), removed→red (target); if an entry is not matched — only the group highlight remains |
| `properties-group-expander.js` | `PropertiesGroupExpander` (FEAT-0029) — auto-expands (only opens, never collapses) the property-panel groups most relevant to the selected element: axis A = changed groups from the diff (`nodeIdToDiffsMap`), axis B = groups characteristic of the element type via the pure `relevantGroupsForElement` (events keyed on `bo.eventDefinitions`, gateway flows, multi-instance, task implementations, non-empty In/Out/Inputs/Outputs). Detects the open state by the `open` class on the header and finds the header by text via the shared `utils.js#findPropertiesGroupHeader` (also used by the highlighter). A missing group is a silent no-op. BPMN only |
| `condition-formatter.js` | `ConditionFormatter` — formatting condition expressions |
| `canvas-viewport.js` | `CanvasViewport` — pan/zoom/fit of the canvas |
| `element-searcher.js` | `ElementSearcher` — full-text search over BPMN elements (FEAT-0006): builds an index from `elementRegistry` and searches ids by name/id/parameters. `buildSearchText(bo)` recursively gathers all primitive values of a moddle object (condition expression and its variables, In/Out mappings, Inputs/Outputs, delegate/class/topic, calledElement, documentation), but does NOT descend into references to other elements (they have their own `id`), otherwise a container would absorb the text of the whole schema. Unlike the viewer's built-in search (name/id only) |
| `search-panel.js` | `SearchPanel` — the floating search panel of the BPMN differ (FEAT-0006 + BUG-0007). Opens with Ctrl/Cmd+F, matched by `event.code === 'KeyF'` (layout-independent → works on a layout where the built-in search broke) + `preventDefault` suppresses Chrome's native search. Uses `ElementSearcher`, highlights all matches with a `search-match` marker, centers the current one (`canvas.scrollToElement`), selects it (`selection.select` → the properties panel shows its parameters) and marks it `search-match-current`; navigation ◀/▶ (Enter/Shift+Enter), a counter, Esc — close. The index is rebuilt after every import (`rebuildIndex`) |
| `process-file-index.js` | `ProcessFileIndex` — the processId→file index (a walk of the whole repository tree + a cache in localStorage + deep XML parsing). Now used only as a **fallback** in `CallActivityLocator`; to be removed once blob-search proves reliable |
| `call-activity-locator.js` | `CallActivityLocator` — resolves processId→{filePath,fileName} for "drilling into" a Call Activity. Primary path: a pinpoint GitLab blob-search by `<bpmn:process id="...">` (only the needed file is loaded, an in-memory cache by ref+processId); fallback — `ProcessFileIndex`. `blobSearchPageUrl` — a link to the GitLab search if the file is not found. Mirrors `handler-locator.js` |
| `caller-locator.js` | `CallerLocator` — the reverse of `CallActivityLocator`: resolves the diagram's own process id(s) → the BPMN files that **call** it (a targeted blob-search for `calledElement="<processId>"`, unioned across ids, cached per ref+ids, current file excluded). For the back-navigation caller picker (FEAT-0023). `resolveCallers` returns `[]` for a root diagram but **throws** on a failed search, so the UI distinguishes "no callers" from "couldn't check". `selectCallers` (pure) is unit-tested |
| `differ-tab-navigator.js` | `DifferTabNavigator` — shared opener-tab navigation (FEAT-0005, extracted from `BpmnDiffer`), used by both orchestrators: `openNestedDiffer(params, fileName)` opens a nested differ tab, picking the differ kind (BPMN vs DMN) by file extension and supplying the resource-href lookup the fresh `about:blank` tab needs (no `chrome.runtime.getURL`); `focusOpenerAndClose()` brings the opener tab forward without reloading it and closes this one (dive out to where we came from); `navigateOpenerTab(url)` navigates the opener tab (BPMN handler navigation). Cross-tab dedup (BUG-0017): `registerTab(identityKey)` joins a `BroadcastChannel` registry and takes a stable identity-derived `window.name`; `focusExistingDifferTab(identityKey)` asks the channel whether any open tab already shows that diagram and, if so, brings it to the front by name instead of opening a duplicate — so `#openDifferForFile` in both orchestrators reuses an open tab no matter how the user got there (the identity key is `DifferParams.identityKey()` = refs + file path) |
| `back-navigator.js` | `BackNavigator` — the toolbar "dive out" control (FEAT-0023, now mounted on **both** the BPMN and DMN differ — FEAT-0005), the mirror of the dive-in `⤵`: a split `⤴`/`▾` button that moves UP the call hierarchy to a calling diagram (it lands on "where we came from" only when we got here by diving in — a coincidence, not the definition). `⤴`: when we dived in, the caller's tab is open above us, so jump straight to it (the injected `onDiveOutToOpener` focuses it — instant, state-preserved); otherwise open the menu. `▾` opens a lazy dropdown of all diagrams that call this one (via `CallerLocator`), the one we dived in from marked and listed first (selecting it reuses its open tab); the rest open fresh (`onOpenCaller`, which auto-selects their call site). Distinct loading/empty/error states. Owns only its DOM; `createElement()` returns null when there is nothing to offer. Unit-tested in jsdom |
| `call-activity-navigator.js` | `CallActivityNavigator` — a minimalist "⤵" overlay on the selected Call Activity (styled like `handler-link`, with a title tooltip). On click it resolves the file via `CallActivityLocator` against the displayed version (`#getShownRef`) and opens its differ; if not found — it opens the GitLab search. During resolution (`#isHandling`) the arrow badge turns into a `.differ-spinner-inline` spinner and returns to an arrow on completion (UX-0008); the badge of any Call Activity selected during an unfinished resolution is also shown as a spinner (`#refreshBadge`) |
| `decision-locator.js` | `DecisionLocator` — the DMN mirror of `CallActivityLocator` (FEAT-0005): resolves a `camunda:decisionRef` → the `.dmn` file declaring it, by a targeted blob-search for `decision id="..."` (no `ProcessFileIndex` fallback). `selectDecisionFile`/`isDmnFile`/`blobSearchPageUrl`, cached per ref+decisionId |
| `decision-navigator.js` | `DecisionNavigator` — the DMN mirror of `CallActivityNavigator` (FEAT-0005): the "⤵" dive-in overlay on a selected `bpmn:BusinessRuleTask` with a `decisionRef`, reusing the `.dive-in-call-activity` badge. On click resolves the file via `DecisionLocator` and opens its DMN differ (the differ kind is chosen by extension in `DifferTabNavigator`); not found / a `${…}` expression → GitLab search |
| `decision-caller-locator.js` | `DecisionCallerLocator` — the DMN mirror of `CallerLocator` (FEAT-0005): resolves this decision's id(s) → the BPMN files whose `bpmn:BusinessRuleTask` calls it (blob-search `decisionRef="<id>"`, current file excluded, unioned, cached per ref+ids). Powers the DMN differ's back-navigation; `resolveCallers` returns `[]` for a top-level decision but **throws** on a failed search. `selectCallers` (pure) is unit-tested |
| `handler-locator.js` | `HandlerLocator` — links a service task to the handler code by a **namespaced key** `topic:<topic>` (external task) \| `class:<SimpleName>` (delegate): `findChangedHandlers` (scans the changed `.kt`/`.java` from the MR changes API → `Map<key, {filePath, diffType}>`, where `diffType` = `added`/`changed`/`removed`; deleted files are scanned on the target-ref, the rest — on the mr-ref) and `resolveLocation(key, ref)` (a dispatcher by prefix → GitLab blob-search → file:line). External task: `@ExternalTaskSubscription("<topic>")` (topic explicit) or `@ExternalTaskBean` (topic from the class name with the first letter lowercased). Delegate: `camunda:class="com.foo.Bar"` → `class:Bar`; `camunda:delegateExpression="${bar}"` → bean → `class:Bar` (resolution — a search for `class <Name>` in the handler file). Matching is by simple name (not FQN) → package collisions are possible; limitations (a non-standard bean, a complex expression, transitive analysis — [FEAT-0015]) — in the file header |
| `handler-navigator.js` | `HandlerNavigator` — a "‹/›" overlay badge on a service task: permanent for tasks whose handler is affected in the MR, colored by `diffType` (green=added, blue=changed, red=removed — like the diff colors; added is visible in the MR version, removed — in the target version, changed — in both), on click (neutral) for the rest. `#getHandlerKey(elem)` builds the namespaced key from the BO (external task / `camunda:class` / `camunda:delegateExpression`). Click: for an affected handler — open its diff in this MR, navigating the source MR tab (`window.opener`) and switching to it via `window.open(url, name)` using the opener's temporary `window.name` (more reliable than `opener.focus()`; fallback — a new tab); for the rest — the code at the currently displayed version in a new tab. A "handler" = an external task handler or a delegate (`camunda:class`/`delegateExpression`) |
| `correlation-locator.js` | `CorrelationLocator` (FEAT-0027) — finds where a message-catching element (`bpmn:ReceiveTask`, message catch/start/boundary event) is woken up in code, by the message **name** (correlation is keyed by name, not id). `extractMessageName(bo)` reads the name (ReceiveTask `messageRef`, event nested `MessageEventDefinition.messageRef`), flagging a `${…}` name as dynamic and excluding throw/end (sending) events. `resolveCorrelations(name, ref)` runs a two-phase GitLab blob-search (cached per ref+name; `per_page=100`). **Exact-term gate:** GitLab Advanced Search tokenises an underscored name (`FINISH_VERIFICATION_API`→`finish`/`verification`/`api`) and quoting is impossible (BUG-0013), so `collectHits` keeps only hits whose snippet literally contains the searched term — otherwise a handler correlating a *different* message is mis-taken for ours. Phase 1 searches the literal and classifies each hit (`correlation` keyword line / `constant` declaration `FOO = "<name>"` / `config` `.yml`,`.properties` / `diagram` `.bpmn`,`.dmn` excluded / `other`). **Phase 2 (case B) resolves a captured constant by fetching its *declaring file* and locating the correlation usage there (`findConstantCorrelationLine`, word-boundary match) — NOT a global search for the constant name:** message constants are routinely named generically (`CORRELATION_MESSAGE`), and a global search floods the result with OTHER messages' correlations (the companion-object idiom keeps declaration + usage in one file). Returns grouped, ranked hits `{ correlation, other, config, tests }`; **test hits (unit + auto-test, any language: `isTestPath` covers `/test(s)/`,`autotests/`,`e2e/` dirs, `*.spec.ts`/`*.test.ts`, JVM `*Test`/`*Spec`/`*IT` classes) are segregated into `tests` and hidden by default** (a future setting may opt them in). The pure orchestrator `resolveWith(name, searchFn, fetchFileFn)` is unit-tested with fakes. Mirrors `handler-locator.js` |
| `correlation-navigator.js` | `CorrelationNavigator` (FEAT-0027) — a "✉→" overlay badge (styled like `handler-link`) on a selected message-catching element. The search runs **lazily on click**, not on `selection.changed`; the badge becomes a `.differ-spinner-inline` spinner while resolving. Display policy: **a single result opens straight away** in a new tab (`blobFileUrl`), no dropdown — whether it is an exact correlation point or the one weaker lead found. Several results → a flat-list dropdown (no group headers): the correlation points if any, else the weaker leads (constant declaration / config), shown plainly. The dropdown mirrors `back-navigator`'s menu (anchored to the badge, close-on-outside-click). A dynamic name → an explanatory note + any config hits; failed search / only-test matches / nothing → a short message (no GitLab-search link — it 404s on this instance) |
| `dmn-differ.js` | `DmnDiffer` — the DMN diff orchestrator + bootstrap (message listener). Back-navigation (FEAT-0005): wires `BackNavigator` with a `DecisionCallerLocator`; `#getCurrentDecisionIds()` reads the decision ids from the shown XML (not the viewer, which renders only the first decision), `#getShownRef()` picks the displayed side's ref, dive-out opens a BPMN caller via `DifferTabNavigator` with `selectCalledProcessIds=<decision ids>` (the caller then selects its Business Rule Task by `decisionRef`) |
| `dmn-differ-view.js` | `DmnDifferView` — the DOM of the DMN differ page (layout, header, buttons). The same visual language as BPMN: a flex toolbar `.differ-toolbar` with groups and icon zoom (UX-0007); there is no properties panel or splitter. The back-navigation group (`BackNavigator`, FEAT-0005, icon `⤴`) is mounted via `setBackNavigator()` before `build()` and rendered just before Close (mirror of `BpmnDifferView`) |
| `dmn-table-viewport.js` | `DmnTableViewport` — zoom/fit/scroll of the dmn-js decision table |
| `dmn-xml-comparator.js` | `DmnXmlComparator` — a semantic comparison of two DMN XMLs → a diff model |
| `dmn-diff-painter.js` | `DmnDiffPainter` — coloring the diff model on the decision-table DOM |
| `gitlab-repo-provider-base.js` | `GitLabRepoProviderBase` — the shared base of both GitLab providers (keeper): `isAvailable`, `init` (+ project id resolution with a cache), `getProjectInfo`/`getChangeInfo`, `isChangeViewActive`, `getBranchFileType`, `findSelectedFilePath`, `extractBranchCommitIdAndFilePath`, `getTargetFilePath` (resolves `old_path` for a renamed file via the MR `/changes` API + the static `extractRenameMap`, cached by URL; no iid / no rename / error → `filePath`, BUG-0002). The collaborators `urlParser`/`domScraper` are ordinary fields (for subclass access); `loadContent` is injected (DI for tests, defaults to `loadFileContent`) |
| `gitlab-repo-provider.js` | `GitLabRepoProvider` — the DOM/heuristic path on top of the base (fallback, "doomed" per REFAC-0001): `initChangeInfo` (MR info from the API URL + cache), `getChangeBranchNames` (via the scraper), `getSourceCommitId` (commits.json → `diff_head_sha`), `getTargetCommitId` (delegates to `MergedMrCommitResolver`) |
| `gitlab-url-parser.js` | `GitLabUrlParser` — pure URL/path parsing (no DOM/network): `parseProject`, `extractMrIid`, `buildMrApiUrl`, `isMrDiffPage`, `getBranchFileType`, `extractBranchCommitIdAndFilePath` |
| `gitlab-dom-scraper.js` | `GitLabDomScraper` — all DOM reads of the GitLab page (the most fragile part): `findSelectedFilePath` (legacy `[data-path]` + `.is-active`/`.diff-file-is-active` and rapid diffs `<diff-file data-file-data>`, gitlab.com — selection by the hash in the URL, otherwise the single bpmn/dmn file), `findBranchCommitIdText` (ref-selector), `getMergeRequestBranchNames`, `findDiffHeadSha`, `isMergedByBadge` |
| `merged-mr-commit-resolver.js` | `MergedMrCommitResolver` — a heuristic for resolving the target commit of a merged MR (the DOM path's role; "doomed"): `resolveTargetCommitId` (branching merged/opened, cache) on top of `MasterCommitManager` and the atom feed of master commits filtered by title |
| `single-entry-cache.js` | `SingleEntryCache` — a generic "last value per key" cache (init/MR-info/target-commit/filteredByTitle) |
| `gitlab-ui-repo-provider.js` | Button injection, file selection; the diff button container is searched by a list of candidate selectors (self-managed without a wrapper, and gitlab.com with `.merge-request-sticky-header-wrapper`). Labels (UX-0009): MR → `Schema diff`/`Decision diff`, repo → `View schema`/`View decision`, secondary → `Diff with local`. The main button carries the `.bpmn-surf-btn-accent` class (defined in `content/content-styles.css`, the brand tone on top of GitLab's native classes); the secondary local-file button is intentionally not accented |
| `repo-provider.js` / `ui-repo-provider.js` | The base provider interfaces. The `RepoProvider` method names are platform-neutral (`isChangeViewActive`, `initChangeInfo`, `getChangeInfo`, `getChangeBranchNames`, `getSourceCommitId`, `getTargetCommitId`, `getDiffSideLabels`, `getTargetFilePath`) — the core is not tied to GitLab/MR. `getTargetFilePath(filePath)` by default returns `filePath` (identity), differs only on a rename (BUG-0002); resolved lazily in `app.js#openDiffer` on click (outside the `mouseup` hot path), `FallbackRepoProvider` delegates to the active provider. `getDiffSideLabels(sourceRef, targetRef)` returns human-readable side labels (`{sourceLabel, targetLabel}`): the provider itself decides whether these are branch names (the whole-MR diff) or a commit message + short id (a single commit selected), and only the ready strings go to the differ; it may be async (if commit metadata needs to be requested) |
| `repo-provider-factory.js` | `createRepoProvider`/`createUIRepoProvider` — the single place that knows about the concrete implementations and their order; the wiring point for new platforms/REFAC-0001. `createRepoProvider` chains `[GitLabApiRepoProvider, GitLabRepoProvider, GitHubRepoProvider]` (the GitHub one inert until subtask 2 — `isAvailable()` is false); `createUIRepoProvider` selects by host (`github.com` → `GitHubUIRepoProvider`, else `GitLabUIRepoProvider` default) |
| `fallback-repo-provider.js` | `FallbackRepoProvider` — a whole-provider fallback: on `init()` it picks the first available and successfully initialized implementation from the ordered list, then delegates all interface calls to it |
| `gitlab-api-repo-provider.js` | `GitLabApiRepoProvider` — resolves MR parameters via the GitLab MR API (`GET /api/v4/projects/{id}/merge_requests/{iid}`, a single cached request). `extends GitLabRepoProviderBase`: implements commit/branch/title resolution (`getSourceCommitId`←`diff_refs.head_sha`, `getTargetCommitId`←`diff_refs.base_sha` — uniformly for opened/merged; `getChangeBranchNames`←`source_branch`/`target_branch`), and inherits the DOM/URL detection methods (file selection, branch-view, project id) from the base. When a single commit is selected (`?commit_id=`), `getTargetCommitId` returns the commit's parent (FEAT-0001), and `getDiffSideLabels` labels both sides with the commit message + short id (from the commits API, cached by sha) instead of branch names — otherwise the target would be labeled with the target branch, which is incorrect. First in the chain (primary); on an MR page `init()` tries the API and, on a missing `diff_refs`/error, returns `false` → `FallbackRepoProvider` falls back to the DOM `GitLabRepoProvider`. There is no per-method fallback |
| `diff-params-builder.js` | `DiffParamsBuilder` — assembles the differ page's neutral parameters (sourceRef/sourceLabel/targetRef/targetLabel/changeRequestId) + a `platform` descriptor `{kind,projectUrl,hostUrl,projectId}`; `sourceLabel`/`targetLabel` are the ready side labels from `getDiffSideLabels` (a branch name or a commit message + short id); the platform specifics are grouped under `platform`, discriminated by `kind` |
| `platform-client.js` | `PlatformClient` — the differ-scope interface (REFAC-0004) for every platform-specific data access the differ page performs; the mirror of the content scope's `RepoProvider` seam. Methods (throw-stubs here): `rawFileUrl(ref, filePath)`, `blobFileUrl(ref, filePath, line?)`, `searchCode(ref, term)` → normalised `{path, line, snippet}[]`, `searchPageUrl(term, ref)`, `prChangedFiles(changeId)` → `{path, oldPath, status}[]`, `prDiffsUrl(changeId)`. Search hits are normalised so a platform's own JSON never reaches the locators (they gate/classify on the neutral `snippet`) |
| `gitlab-platform-client.js` | `GitLabPlatformClient extends PlatformClient` — holds verbatim the GitLab URL/search/changes construction that used to live inline in `DifferParams` and the locators (`/-/raw/`, `/-/blob/`, `/api/v4/.../search?scope=blobs`, `/-/search`, MR `/changes`, `/-/merge_requests/{iid}/diffs`), so GitLab behaviour is unchanged. Constructed from the descriptor's `{projectUrl, hostUrl, projectId}`; fetching still goes through the global cookie-session `loadFileContent` (injectable for tests). Auth-aware fetching is deferred to subtask 3 |
| `platform-client-factory.js` | `createPlatformClient(platform)` — builds the differ-scope client by `platform.kind` (`'gitlab'` → `GitLabPlatformClient`; `'github'` → inert `GitHubPlatformClient`, REFAC-0004 step 1.3; default throws); the differ mirror of `repo-provider-factory.js`. `github-platform-client.js` throws on every method until subtask 2 |
| `version-info.js` | `VersionInfo` — pure version logic (FEAT-0012): numeric `compare`/`isNewer` (not lexical — otherwise `0.9` > `0.18`), parsing `CHANGELOG.md` and selecting entries newer than the installed one (`changesSince`). No DOM/network/`chrome.*`, unit-tested |
| `update-checker.js` | `UpdateChecker` — the network part of the check (FEAT-0012): fetches `version.json` and (if there is something newer) `CHANGELOG.md`, computes the result via `VersionInfo`. The fetchers are injected (DI) → unit tests without the network. GET only, `credentials:'omit'` (no cookies / data sending) |
| `update-service-worker.js` | The service worker of the update mechanism (FEAT-0012): a `chrome.alarms` scheduled check + a check at startup, a badge on the icon, state in `chrome.storage.local`, handling of popup/content messages (`update:getState`/`checkNow`/`setEnabled`/`reload`/`openPopup`/`openUrl`). Respects auto-check being disabled (then it does not touch the network). The version source is configurable (`config.js#UPDATE_*`), with an empty URL it is a no-op |
| `popup/` | The icon popup window (`popup.html`/`popup.js`/`popup.css`, FEAT-0012): the current/latest version, the check time, the explicit source URL + "no data is sent", "What's new" (from the CHANGELOG), "Check now", "Update" (copy `git pull` + "Reload the extension" via `runtime.reload`) and an auto-check toggle. The SW owns the network/state; the popup only displays and sends commands. Works both as an action popup and as a tab (opened from the differ indicator) |
| `update-indicator.js` | `UpdateIndicator` — the shared (BPMN+DMN) "🔔 vX" indicator in the differ page's toolbar (FEAT-0012), modeled on `BranchIndicator`. The differ page has no `chrome.*`, so `updateInfo` arrives in the params from the content script, and a click opens the popup as a tab (`window.open` by `popupUrl` = `chrome.runtime.getURL('src/popup/popup.html')`). The views call `setUpdateInfo()` before `build()` |
| `models.js` | DTOs: `FileType`, `ProjectInfo`, `MergeRequestInfo` |
| `utils.js` | DOM, HTTP, XML parsing, script loading |
| `config.js` | Constants (e.g., `MASTER_BRANCH_NAME`) |
| `file-type-detector.js` | Determining the file type by extension |
| `camunda-bpmn-moddle-manager.js` | Loading/caching the Camunda moddle |
| `master-commit-manager.js` | The master commit history with a cache in localStorage |
| `page-reloader.js` | Protection against reloads (max 3 attempts) |
| `manifest.json` | The MV3 manifest; the content_scripts order is critical |
| `libs/` | Vendored dist files of external libraries; generated by `npm run sync:libs`, do not edit by hand (see docs/conventions.md) |
| `scripts/sync-libs.js` | Copies library files from `node_modules` into `libs/` by the versions in `package.json` |
| `test/` | Unit tests (`node:test` + jsdom), the layout mirrors `src/` (`src/<path>/x.js` → `test/<path>/x.test.js`): the vm harness `test/support/scope.js` (included as `require('#scope')`), the fixtures `test/fixtures/` — see docs/testing.md |
